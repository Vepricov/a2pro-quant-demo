# План демонстрации

## Вердикт по ТЗ
Демонстрация «квантуем → инферим сжатую → сохраняем → поднимаем в платформе»
**попадает в ТЗ** как реализация направления «методы эффективного хранения
больших моделей» (ПМ «А2.Оптимизация») и как «воспроизведение существующих
техник» (ТЗ3 §3.2/3.3). Чтобы быть защитимой на приёмке, добавлены: метрики
качества (perplexity) + ресурсные метрики (диск/VRAM/latency), save→reload,
JSON-артефакты. Детальное соответствие — в `README.md`.

## Шаги

- [x] 1. Каркас проекта: config (JSON, frozen dataclass), recipe-registry,
      quantize, evaluate, metrics, serve_client, cli, docker-compose.
- [x] 2. Конфиги для Qwen2.5-7B: `w8a8` (SmoothQuant+GPTQ), `w4a16` (GPTQ).
- [x] 3. Локальная проверка: синтаксис + парсинг конфигов (без GPU). ✓
- [ ] 4. brain_lab: `uv` окружение, установка `llm-compressor` + vLLM.
      → verify: `python -c "import llmcompressor, vllm"`.
- [ ] 5. `compare` на Qwen2.5-7B W8A8.
      → verify: `results/results_table.md` показывает FP16 vs quant
      (диск ~↓2x, VRAM ↓, ppl рост в пределах ~0.1–0.5).
- [ ] 6. Поднять vLLM по OpenAI-эндпоинту на квантованном чекпоинте.
      → verify: `serve_client.py` возвращает осмысленный ответ; `/health` ok.
- [ ] 7. (опц.) W4A16 как вторая точка сравнения; SmoothQuant отдельной веткой.
- [ ] 8. Свести артефакты под ПМИ: JSON-конфиги, логи, таблицы, размеры на диске.

## Открытые вопросы
- Какая именно модель «в контуре» (Qwen2.5-7B-Instruct по умолчанию; уточнить
  версию/чат-шаблон, если кастом).
- Какой GPU на brain_lab выделить (1×A100-80GB достаточно для 7–8B + квантизации).
- Нужен ли проброс реального закрытого чекпоинта вместо HF-модели.

## Команда сборки эксперимента (когда дадут go на сервер)
```bash
ssh brain_lab "cd a2_pro_kvant && uv venv && uv pip install -e . && \
  tmux new -d -s kvant 'scripts/run_compare.sh configs/qwen2.5-7b-w8a8.json'"
```
